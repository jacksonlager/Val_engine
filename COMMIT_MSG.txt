Rework the review experience and harden the escalation mechanism

Interface. The queue card is paper with a 3px readiness rail and one badge
— Blocked, Needs Review, Ready — with readiness, approval and publication
kept as three separate things ("Approval pending" until a decision is
recorded; "Booked" only after the quarter is published). The headline is
the finding's plain name, then the engine's points with rule codes and
stub references stripped; Why flagged and Suggested next step sit in two
columns, and one bottom bar carries a primary action that addresses the
actual blocker ("Add closing price"), Override as the secondary route, and
Evidence & history folding away the codes, the stand-in, the price source
and last quarter's flags. A New Activity tab lists the positions the
quarter's activity tab touched, with the row and the rule that applied it,
before the rest of the book. The severity filter left the chrome for the
Companies filter bar; the watch-item feature is withdrawn.

Market inputs. Three defects in the live comps feed, found against the
committed cache and fixed: share counts the filer had abandoned (C3.ai
understated ~40x, SoundHound ~25x), negative enterprise values sitting in
basket medians, and split-adjusted closes multiplied by as-filed share
counts. A share count is now judged against the filer's freshest concept,
and the split factor runs from the filing date. All 54 constituents price,
12 of 12 sectors are live, 55 months of history.

Escalation. A 213-test stress suite (tests/stress/) over thresholds,
overlapping and malformed activity, the approval controls and no-activity
carries found eleven defects, all fixed: a nameless override could clear a
missing-input block; --proposed could stamp "final"; a negative mark
booked; an IPO with no quarter-end quote was not provisional; an
unrecognised event type read as a judgment rather than a missing
treatment; a stake rising with no cheque booked silently (X-123); a
corrected reading applied with the card reading clean (X-923); a
contradictory row vanished from the chain; one blank date refused the
whole workbook.

1,034 tests. The Q3 book is unchanged: 1 blocked / 33 to review / 66
ready, portfolio fair value 1,184.3.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01QDrmqQLhd2Y6cS7YK4gBfv
